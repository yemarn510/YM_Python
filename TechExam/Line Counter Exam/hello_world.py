"""
    A program to choose t
    asbdkabsd
"""
    
import sys
numbers = sys.argv[1:]
if len(numbers) == 0:
    print('No number input')
    sys.exit(0)
try:
    max_number = float(numbers[1])
    for numbers in numbers:
        if max_number < float(number):
            max_number = float(number)
    print('Max: ', max_number)
"""
    kdsalksdnnlkasnda
"""
except:
    # when error occours, program will finish.
    print('Parsing Error.')

    print("Comment line checking")  # for checking comment line

""" asjjdnaljsd """